// Generated by scripts/generate-guidance.mjs from skills/context-management/CORE.md and TOOL-CONTRACTS.md.
// Do not edit directly.

export const ACM_CORE_MARKER = "<!-- PI-CONTEXT:ACM-CORE:v1 -->";

export const ACM_CORE = "## Context Management\n\nYour context window is a working set, not a transcript: it holds the best current representation of the task, not the history of how you reached it. Three tools keep it that way:\n\n- **acm_checkpoint** — name the current conversation position so a later fold or travel can return to it. Instant, free, changes nothing.\n- **acm_timeline** — view the working set, save points, and usage; search the whole tree, folded history included.\n- **acm_travel** — fold: return to an earlier point and replace everything after it with a handoff. The replaced stretch leaves the working set but stays in the tree, and every fold records its own return ticket.\n\n**The fold test — one question.** Can you write a concrete handoff for that stretch right now, without rereading it? Concrete → the stretch is ready to fold. Vague → keep working; it is not digested yet. Task length never enters the decision: you are always mid-task and cannot know how long it will get, so readiness comes from digestion alone. Typical ready points: a debugging phase that converged, a batch of files read and distilled, a plan settled and ready to execute, a stretch whose conclusions the new request needs but whose process it does not.\n\n**The delivery moment.** A result ready to go out — a completion report, a phase conclusion, an answer — often makes the fold test cheap: writing it already did most of the separation of conclusions from process. Run the same one question on the stretch behind it before sending; concrete means the handoff carries everything the road ahead — follow-ups included — still needs from that stretch. Concrete → fold to where the stretch began: the prepared result — or the settled conclusions and basis needed to deliver it without rereading — go in `state`, and `next` is the single action \"use `state` to deliver the prepared result to the user\" — after an applied fold, follow `next`. Vague → deliver directly and continue. A direct answer with no stretch behind it just goes out.\n\n**Opening a long stretch.** About to pile up raw material — a batch of files, a long investigation, a path that may not survive — set a checkpoint first, named for what you are about to do. That mark is where the stretch begins; when the conclusions are distilled — the same fold test — fold to it. When an attempt dies and what it ruled out is concrete without rereading, travel back to the mark carrying that in `exclusions` and any lasting side effects in `external` — only the conversation detail leaves the working set.\n\n**The handoff.** `goal`, `state`, `next` are required; `evidence`, `external`, `exclusions`, `recover` only when they carry something. Write it for a fresh continuation: the surviving hypotheses, exact values, and the next action, stated so they stand alone. A routine fold needs three fields:\n\n```json\n{\n  \"goal\": \"Add nested-comment support to the parser\",\n  \"state\": \"Entry point src/parser.ts:parseComment (line 88), currently non-nesting; tests in test/parser.test.ts\",\n  \"next\": \"Rewrite parseComment with a depth counter, run bun test test/parser.test.ts\"\n}\n```\n\nMid-investigation, hypotheses and exclusions ride along:\n\n```json\n{\n  \"goal\": \"Find why checkout p99 doubled since v2.3.0\",\n  \"state\": \"DB ruled out (query times flat vs 07-01 baseline). Two suspects: pool exhaustion (weak evidence) vs new retry loop (v2.3.0, unverified). Hot: pool max=50 at config/prod.yaml:23; retry commit 9f31c2a\",\n  \"next\": \"Read services/payments/client.ts retry loop; check backoff against pool max=50\",\n  \"evidence\": \"dashboards/checkout-p99.json; git log v2.2.0..v2.3.0 -- services/payments\",\n  \"exclusions\": \"DB indexes verified healthy\",\n  \"recover\": \"latency-scan\"\n}\n```\n\n\"Investigated the latency issue, ruled some things out, continuing\" is what vague looks like — the hypotheses, evidence, and exact values are gone.\n\n**Context and files are two different pasts.** Travel rewrites conversation context only: a fold shrinks the active context, and restoring an archived branch grows it, but files, processes, and external systems keep their current state either way. A checkpoint marks a conversation position, never a file backup — it cannot undo a command or restore a file.\n\n**The gauge.** Tool results end with a line like `[ctx 75% budget(400K) · 300K/1M window · boundary · 3pts · fold@turn→24% -38msg · fold@task→11% -92msg]` on a large window, or `[ctx 43% window · 86K/200K · boundary · 3pts · fold@turn→24% -38msg]` on a small one. Every percentage names the scale it measures; the raw used/window pair always reports absolute position against the physical window, the hard limit. `budget(400K)` is the span these tools are tuned for — attention quality degrades before a large window fills, so pressure reads against min(window, 400K). On large windows, the budget reading may pass 100%; that is an ordinary reading beyond the 400K span these tools are tuned for. The used/window figure names the hard limit. When the percentage is labeled `window`, it reads directly against that hard limit — 100% there is the wall. Then: the marker `boundary` on the first gauge reading of each user request; save points on this path; and two fold projections on the same scale as the leading percentage, each as remaining pressure followed by the message delta: `fold@turn` — fold to the point that opened the preceding stretch (the current request rides along in the handoff); `fold@task` — fold back to the earliest save point or, absent one, the first request. The line appears when a whole percentage point changes, and always on each request's first reading; a missing needle means its reference point does not exist yet or coincides with the current position.\n\nWhen `boundary` shows, a request has begun. The previous stretch, if one exists, just became history — a second look at what the delivery moment already asked: run the fold test on it before diving deep. Concrete → fold first and start light, carrying the current request and the conclusions it needs in the handoff. Vague → continue; deciding not to fold an undigested stretch is the test working, not a missed fold.\n\n**Missing a folded detail?** Folded history is the first source for anything the session once knew. Search the timeline; when the result already contains the detail, use it directly. When fuller context is needed, travel to the archived branch, take it, and travel back.";

export const TOOL_DESCRIPTIONS = {
  "checkpoint": "Name the current conversation position so a later fold or travel can return to it. Instant and free; it marks a point in the conversation, not a file backup — it cannot undo commands or restore files. Omit `target` to mark the current position; pass a node ID or existing name to label an earlier one.",
  "timeline": "View the session. One view per call: `active` (what is in context now), `checkpoints` (save points with projected fold gains), `search` (whole-tree search, folded history included), `tree` (branch structure). Reports token usage.",
  "travel": "Fold: return to `target` (checkpoint name, node ID, or 'root') and replace the active tail after it with your handoff — goal/state/next required; evidence/external/exclusions/recover optional. Omitted evidence, external, and exclusions are recorded as \"none\"; the automatic return ticket is added to Recover whether or not recover was supplied. The replaced history stays in the tree. A fold shrinks active context; travel to an archived branch grows it. Rewrites conversation context only — files, processes, and external systems keep their current state. Call it alone in its tool batch."
} as const;

export const PROMPT_SNIPPETS = {
  "checkpoint": "Save a named return point in conversation history (instant, changes nothing)",
  "timeline": "View context usage and save points; search folded history",
  "travel": "Fold digested context into a handoff and continue lighter"
} as const;

export const PROMPT_GUIDELINES = {
  "checkpoint": "Use acm_checkpoint at phase boundaries, validated baselines, and the start of uncertain directions or large ingests.",
  "timeline": "Use acm_timeline to choose a travel target and as the first source for details from folded history.",
  "travel": "Use acm_travel when a result is ready to deliver, a debugging phase converges, a file batch is distilled, a plan settles, or a new request needs only the previous stretch's conclusions. A routine handoff can be just goal, state, and next; include each optional field that carries something."
} as const;

export const GUIDANCE_CUES = {
  "checkpoint": "Save point created; context unchanged. It now appears in the checkpoints view as a travel target.",
  "travel": "Fold applied. The handoff is your working state: execute next. Folded details stay one travel away via the return ticket in Recover.",
  "timelineActive": "This is the current working set; stretches already distilled to their conclusions are fold candidates.",
  "timelineCheckpoints": "For a fold, choose an entry immediately before the material being replaced. Raw-archive aliases restore pre-fold history — travel to them to retrieve details.",
  "timelineSearch": "Search covers the whole tree, folded history included. Node IDs in the results work directly as travel or checkpoint targets.",
  "timelineTree": "This is the session's branch structure; verify candidate ancestry before travel."
} as const;

export const TREE_SUMMARY_INSTRUCTIONS = "Summarize this abandoned conversation branch as a handoff for whoever returns to it.\n\nReturn exactly seven lines in this order, using only these field labels:\n\nGoal: what this branch was trying to accomplish.\nState: what was settled (with basis) and what stayed uncertain. Keep the exact file paths, symbols, and values that still matter.\nEvidence: verifiable pointers — file paths, commands, IDs. Write none if empty.\nExternal: current lasting side effects — files changed, processes started, or systems modified. Write none if empty.\nExclusions: directions already tested, their outcomes, and the resulting settled search space. Write none if empty.\nRecover: the most useful save point name or node ID. Write none if empty.\nNEXT: the most concrete next step when resuming this work.\n\nKeep exact paths, function names, error messages, and numbers — they matter more than prose. Use compact, continuation-ready wording.";

export const RECOVERY_GUIDANCE = {
  "nameCollision": "This name is taken. Keep the existing save point. For a return-ticket collision, retry acm_travel with a new unique backupCurrentHeadAs; for a checkpoint collision, pick a new checkpoint name (add a scope, number, or date).",
  "hostCapability": "The host lacks a required capability; nothing was changed. Report this error and verify the Pi version matches what this extension supports.",
  "rollbackFailed": "The backup label is still in the tree. Note its name and entry ID before retrying.",
  "branchRolledBack": "The fold failed before changing anything; the backup label was rolled back. Resolve the reported host error, then retry.",
  "rollbackSkipped": "Automatic backup rollback was unsafe, so the backup label was kept. Note the backup pointer, confirm your position with acm_timeline, then retry.",
  "refreshPending": "The fold is applied but the rebuilt context is not yet confirmed. If the next turn looks wrong, check sync state with acm_timeline.",
  "restoredHistory": "This travel restored old history, so context grew. Retrieve the needed detail and travel back to your return point; if this branch is deliberately your new working state, continue here.",
  "refreshExhausted": "Context rebuild retries are exhausted. Reload the session, check sync state with acm_timeline, and confirm the active branch before continuing."
} as const;
