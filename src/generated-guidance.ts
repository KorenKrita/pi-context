// Generated by scripts/generate-guidance.mjs from skills/context-management/CORE.md and TOOL-CONTRACTS.md.
// Do not edit directly.

export const ACM_CORE_MARKER = "<!-- PI-CONTEXT:ACM-CORE:v1 -->";

export const ACM_CORE = "## Agentic Context Management CORE\n\nCompression is intelligence: to understand something is to be able to restate it shorter without losing what matters. Your context window is a **working set**, not a transcript — it should hold the best current representation of the task, not the history of how you reached it. The ACM tools (`acm_checkpoint`, `acm_timeline`, `acm_travel`) let you act on that understanding. They are yours to use autonomously, as ordinary as reading a file; only an explicit user request to hold travel pauses it, and only for the scope the user names.\n\n### What earns a place in the working set\n\n- Settled conclusions, decisions, and constraints.\n- Honest uncertainty: unknowns kept as unknowns, competing hypotheses with the evidence for each and the next test that would tell them apart.\n- The **hot set** — exact values, identifiers, snippets, and wording the next few steps will reuse verbatim.\n- Pointers — paths, commands, commits, node IDs, checkpoints — to everything else.\n\nRaw process whose outcome is already extracted — logs read, diffs applied, searches concluded, dead ends understood — is **sediment**: it competes for attention without changing any future decision. Removing it deletes nothing; history stays in the session tree, reachable by pointer.\n\n### The moves\n\n- **Save** — `acm_checkpoint` labels the current state so it can be found again. It never blocks, branches, or folds anything. Save when returning here later has real value: before a risky attempt, at a validated baseline, before a fork in strategy, when parking one front to work another, before folding raw history away. Recoverability is what makes bold compression and bold exploration cheap. File backups protect the disk; a checkpoint protects this conversation — they never substitute for each other; a risky step deserves both.\n- **Orient** — `acm_timeline` shows the spine, save points, summary depth, usage, and sync state. It reports facts; what they justify stays your call.\n- **Fold** — `acm_travel` replaces lived process with its **handoff**, and is as recoverable as a save: the raw path stays in the tree behind a pointer, one travel away. Fold when low-attention-value, high-noise material has a substantially more concise representation and the expected attention gain exceeds transition and continuity cost. **Cold start** is the handoff integrity test: knowns remain known, uncertainty remains open, current obligations survive, and `next` is executable. Mid-investigation travel can be valuable when those conditions hold. Target the last clean point before the material being folded, not the nearest label — **anchor gravity** misleads.\n- **Rebase** — a fold to an earlier base. When summaries stack or start competing over what is authoritative, merge everything that survives into one handoff at the earliest base that passes cold start without growing projected summary depth. Root is a candidate, never a default.\n- **Rehydrate** — travel toward an archived branch to recover one exact detail. Save your return point first, fetch the detail, then travel back carrying the extract. Compression stays reversible; that is why it can be bold.\n- **Fork** — save the fork point, explore one direction freely, and either fold the winning path forward or travel back to the fork carrying what the failed direction proved in Exclusions.\n\n### Cadence\n\nCompress continuously — integrate observations into conclusions as you work. Fold in batches — each travel costs a tool round-trip and one summary layer, so commit representation gains in meaningful units, not after every step.\n\nTwo failure modes frame the healthy band. **Sediment**: a better representation exists but the raw process still occupies the working set. **Thrash**: folding tiny deltas, then immediately rereading what was just folded. Between them the band is wide, and different models legitimately choose different batch sizes inside it.\n\nACM Judgment is independent of a full gauge: sediment can cloud attention at low usage, while high usage can still contain details whose attention value remains high. A comfortable cruise — around a third of the working budget (the smaller of the model window and 400K) — is a preferred outcome, not move authorization. Correctness, task continuity, Representation Gain, and cold start determine the result; native compaction remains available when genuinely long work outruns useful travel.\n\nUse ACM Judgment as a standing lens. Distilled reads, rejected directions, completed phases, new requests, possible return points, stacked summaries, missing archived details, and rising pressure are signals to evaluate the working set — not predetermined moves. Check for a Compression Candidate, Compressibility, Attention effect, Recovery value, and Transition effect; then choose the net-positive move or continue with the current working set.\n\n### The handoff\n\n`acm_travel` accepts a structured handoff with seven semantic fields: `goal`, `state`, `evidence`, `external`, `exclusions`, `recover`, and `next`. Supply every field; write `none` only for empty supporting fields. Runtime owns the durable text format.\n\n`state` carries live cognition, not only results: knowns, unknowns, hypotheses, surviving fronts, and the hot set. `next` is one concrete action a fresh agent could execute immediately. A fold mid-investigation:\n\n```json\n{\n  \"goal\": \"Find why checkout p99 latency doubled since v2.3.0.\",\n  \"state\": \"Not the database — query times flat vs 2026-07-01 baseline. Two hypotheses: pool exhaustion (errors correlate, evidence weak) vs new retry loop in payments client (added in v2.3.0, untested). Hot: pool max=50 in config/prod.yaml:23; retry commit 9f31c2a.\",\n  \"evidence\": \"dashboards/checkout-p99.json; git log v2.2.0..v2.3.0 -- services/payments.\",\n  \"external\": \"none\",\n  \"exclusions\": \"DB indexes — verified healthy, do not revisit.\",\n  \"recover\": \"latency-hunt-scan\",\n  \"next\": \"Read the retry loop in services/payments/client.ts and check backoff bounds against pool max=50.\"\n}\n```\n\n### Facts and receipts\n\nA tool call is a request; only its matching result is fact — applied, not applied, or indeterminate. Read the mutation receipt once. After an applied travel, trust the handoff as your authoritative current state and execute `next`; verify only uncertainty recorded in the handoff or facts changed by later independent activity. Travel rewrites conversation context only: files, processes, and external systems keep the state recorded in `external`.";

export const TOOL_DESCRIPTIONS = {
  "checkpoint": "Save point: attach a semantic label to a session node without changing the active context. Never blocks or folds anything — it makes recovery, forks, and bold compression cheap. Use before a risky attempt, at a validated baseline, before a fork in strategy, when parking a front, or before raw history is folded away. Names are unique and case-sensitive across the whole session tree ('root' is reserved); name the state a future search should find. Omitting `target` labels the nearest meaningful USER/AI turn; an explicit checkpoint name or node ID labels older history.",
  "timeline": "Orient: inspect the session tree and context economics through one view — `active` (default; the spine the model actually sees), `checkpoints` (save points with projected post-travel summary depth), `search` (find labels, node IDs, or content across the whole tree), or `tree` (topology, when ancestry or branch ownership matters). The HUD reports usage, ACM pressure, summary depth, and sync state. Timeline reports facts; which fold, target, or cadence they justify stays your judgment.",
  "travel": "Rewrite the working set through one recoverable transition: fold finished process into its handoff, rebase stacked summaries onto an earlier base, or rehydrate an archived branch. The handoff must pass cold start — a fresh agent could continue from it and its pointers alone — carrying knowns, open unknowns, the hot set, and one executable NEXT. Target the last clean node before the material being folded; for a rebase, prefer the earliest base whose projected summary depth does not grow. Travel must run alone in its assistant tool batch. The result is the only fact: applied, not applied, or indeterminate."
} as const;

export const PROMPT_SNIPPETS = {
  "checkpoint": "Save a recoverable point without changing context",
  "timeline": "Inspect the session tree, usage, and travel evidence",
  "travel": "Fold, rebase, or rehydrate the working set through a cold-start handoff"
} as const;

export const PROMPT_GUIDELINES = {
  "checkpoint": "acm_checkpoint is cheap and never mutates context: save before risk, forks, baselines, and folds. Names are recovery cues, not workflow states.",
  "timeline": "acm_timeline reports facts — spine, save points, summary depth, usage. Judgment about folding, targets, and cadence stays with the agent.",
  "travel": "acm_travel is a normal working move — fold finished process into a handoff as readily as you save a checkpoint; only an explicit user request to hold travel suspends it, and only for the scope the user names.\nAn acm_travel fold is context hygiene, not the turn's deliverable: if an answer is owed, deliver it — a fold's NEXT carries the still-undelivered work, and recording it in State is not giving it.\nRun acm_travel alone in its assistant tool batch, and read its result before building on the new context."
} as const;

export const GUIDANCE_CUES = {
  "checkpoint": "Save point applied; the working set is unchanged. This state is now cheap to return to and acts as an activation foothold for later orientation. Use timeline when topology evidence would improve the next judgment; choose any later travel target by the material it precedes, not merely by this checkpoint.",
  "travel": "Travel applied: this result is the mutation receipt, and the handoff is now the authoritative working set. Execute NEXT directly. Trust the handoff's known state; verify only uncertainty it explicitly carries or facts changed by later independent activity. Rehydrate only when one exact missing detail is actually needed.",
  "rebaseCheck": "This spine already carries handoff layers; the next fold would stack another. Rebase instead: merge surviving state into one handoff at the earliest base that passes cold start without growing projected depth. Root is a candidate, never a default.",
  "timelineActive": "`active` is the spine the model sees. Evaluate whether any low-attention-value, high-noise material has a substantially more concise representation, then compare attention and recovery gain with transition and continuity cost before choosing the next move.",
  "timelineCheckpoints": "`checkpoints` lists save points with projected post-travel depth. Choose a target by what it precedes, not by how recent or well named it is — anchor gravity misleads.",
  "timelineSearch": "`search` spans the whole tree. Narrow until the last clean node before the material being folded is identifiable; a raw node ID from this result is a valid target.",
  "timelineTree": "`tree` shows ancestry and branch ownership. Reject targets inside the material being folded or on another front, then return to a narrower view."
} as const;

export const TREE_SUMMARY_INSTRUCTIONS = "Summarize this abandoned conversation branch as a handoff for whoever returns to it later.\n\nWrite exactly these seven slots, once each, in this order, each starting its own line, with no other headings:\n\nGoal: what this branch was trying to accomplish.\nState: what was settled here, with the evidence that settled it, and what stayed uncertain, marked as such. Include the exact files, symbols, and values still in play.\nEvidence: pointers a reader can verify directly — file paths, commands, IDs. Write 'none' if empty.\nExternal: lasting side effects outside the conversation — files changed, commands run, systems touched. Write 'none' if empty.\nExclusions: directions tried and closed here, so a retry does not repeat them. Write 'none' if empty.\nRecover: the most useful save point or node ID to return to. Write 'none' if empty.\nNEXT: the single most concrete next action if this work resumes.\n\nPreserve exact file paths, function names, error messages, and numbers; they outrank prose. Keep the whole handoff compact.";

export const RECOVERY_GUIDANCE = {
  "nameCollision": "Search existing checkpoints, preserve the semantic base, and add the smallest useful scope, ordinal, or date. Do not overwrite the existing recovery target.",
  "hostCapability": "The supported Host Bridge capability is unavailable or malformed. Stop mutation and report the named capability error; verify the exact supported Pi version before retrying.",
  "rollbackFailed": "The backup label remains in the tree. Record its label and entry ID as a recovery pointer before any retry.",
  "branchRolledBack": "Branch creation failed before mutation; the new backup label was rolled back. Correct the reported host failure before retrying.",
  "rollbackSkipped": "Branch mutation or prior aliases make automatic backup rollback unsafe. Keep the reported backup pointer and inspect the active leaf before retrying.",
  "refreshPending": "Travel mutation landed, but rebuilt message evidence is pending. Use the reported summary entry as the fallback and inspect context sync state if the next rebuild fails.",
  "restoredHistory": "Off-path travel restored raw history. Take the exact detail this rehydration came for, then travel back to the summary branch unless this branch intentionally becomes the new working set.",
  "refreshExhausted": "Context reconstruction exhausted bounded retries. Reload the session, inspect timeline sync state, and resume only after the selected branch is authoritative."
} as const;
