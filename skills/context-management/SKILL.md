---
name: context-management
description: "Recover advanced ACM mechanics when handoff serialization, target selection, travel isolation, archive rehydration, or mutation/context-sync evidence is ambiguous. Use only after normal representation and cadence judgment identifies the mechanic."
---

# Advanced Context Management

Route the one active mechanic to its authoritative reference:

- Load [Handoff Wire Format](references/handoff-wire-format.md) when a representation update is ready to commit but the seven-slot handoff, hot set, uncertainty fidelity, surviving fronts, or continuation fidelity remains ambiguous.
- Load [Advanced Target Selection](references/target-selection.md) when the compression seam is known but the last clean pre-seam anchor remains ambiguous because of ancestry, interleaved fronts, older or missing labels, raw-node fallback, or checkpoint-name collision.
- Load [Travel Isolation](references/travel-isolation.md) immediately before a travel call when sibling tool work exists, or after `mixed_tool_batch` reports that isolation was violated.
- Load [Archive Recovery](references/archive-recovery.md) when one exact archived value, wording, error, or decision must be rehydrated into the authoritative representation.
- Load [Exceptional Recovery](references/exceptional-recovery.md) when a result reports mutation failure, backup rollback failure, indeterminate branch state, exhausted context refresh, restored history, or another host outcome that makes the active representation uncertain.

Load one reference at a time. When its completion criterion is met, return here and either act from CORE judgment or replace it with the next reference whose condition is now active. Routing is complete when the current mechanic has one authoritative reference and no unrelated playbook remains in the working set.
