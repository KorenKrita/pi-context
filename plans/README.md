# Implementation Plans

Generated by the improve skill on 2026-08-14（基准 commit `858d25cd`）。两次审计（架构深化视角 + 九类别独立复扫）合并去重后，经用户裁决选定 5 项；复盘管线相关（注入围栏、YAML 汇总仪表盘）按用户指示搁置。Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | 统一三处锚点扫描为 scanner module 并共享会话快照 | P1 | M | — | DONE (`894457a0`..`0021a59f`) |
| 002 | tool-protocol 未配对剥离改为 batch-local 配对（先复现后修） | P1 | S | —（与 001 文件不相交，可并行） | DONE (`2142c962`) |
| 003 | Bun 版本契约写进开发者文档 | P2 | S | — | DONE (`d93a8866`) |
| 004 | 按概念拆分 lib.ts 并删除 | P2 | M | 001 | DONE (`39ea1627`) |
| 005 | 抽取 provider delivery 状态机 | P2 | M | 004 | DONE (`fb79f1e7`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 先行：它改动 checkpoint/travel/lifecycle/timeline/context-packet/host-bridge；004 的搬家与 005 的 runtime 重写都应在 001 的成品上进行，避免同文件双向冲突。
- 002 与 001 完全不相交（tool-protocol.ts + 其测试），可并行执行。
- 003 随时可做（纯文档）。
- 链条：001 → 004 → 005。005 明确不做 gauge/pressure 抽取（见其「范围修正」）。
- 全部落地后跑一次 `bun run verify:acm`（含真实 Pi host fixture 的完整 gate）。

## Findings considered and rejected

- timeline「输出未净化会话文本」（SECURITY 候选）：误报——`timeline-tool.ts:428` 的 renderResult 已对全部 result 文本 `sanitizeTerminalText`，`entryText` 原文属 model-facing 内容，原文即功能。
- 「compaction 锚点扫描缺 inspected++、window 失效」：证伪——`runtime-lifecycle.ts:862`（858d25cd 时 :533）循环头 `index--, inspected++` 存在，window 生效。
- review 管线注入围栏（SECURITY-02）与复盘 YAML 汇总仪表盘（DIRECTION-01）：真实成立，但用户裁决搁置（涉及复盘的都不急）。
- GaugeCycle 独立 module（架构审查候选 2 的一半）：005 全文读码后砍掉——gauge 方法是 `context-gauge.ts` 深模块上的 3 行转发，再包一层是 shallow module，删除测试不通过。
- travel 回执装配抽取 + fold 投影三处统一（架构审查候选 3）：未入选本轮；001 的快照已顺带解决 checkpoints 视图的性能面，文案/装配重构另议。
- AcmSessionRuntime 全类拆分收敛为只抽 ProviderDelivery：见 005「范围修正」。
- `src/lib.ts` deep import 兼容性：该文件从来不是声明 API——package.json 的 pi manifest 只暴露 `./src/index.ts` 一个扩展入口，仓库文档也从未承诺 src 内部路径稳定。拆分（004）对**行为**零变化，对 deep import 属于显式不支持面；如外部确有需求，可在 src/lib.ts 位置恢复纯 re-export 薄层（已评估，非必要不引入）。
