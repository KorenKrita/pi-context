{
  "name": "pi-context",
  "version": "2.1.0",
  "description": "Agentic Context Management for Pi — checkpoint, timeline, and time-travel tools",
  "files": [
    "src",
    "skills",
    "README.md",
    "AGENTS.md",
    "implementation-notes.html"
  ],
  "scripts": {
    "test": "bun test",
    "generate:guidance": "bun scripts/generate-guidance.mjs",
    "test:guidance": "bun test scripts/generate-guidance.test.mjs test/guidance-quality.test.ts",
    "test:host": "cd test/host-fixture && bun run verify",
    "typecheck": "tsc --noEmit",
    "verify:acm": "bun scripts/generate-guidance.mjs --check && bun run test:guidance && bun run typecheck && bun run test:host"
  },
  "keywords": [
    "pi-agent",
    "pi-package",
    "context-management",
    "context-optimization",
    "time-travel",
    "checkpoint",
    "pi-skill"
  ],
  "type": "module",
  "author": "KorenKrita",
  "license": "MIT",
  "devDependencies": {
    "@earendil-works/pi-agent-core": "0.80.6",
    "@earendil-works/pi-ai": "0.80.6",
    "@earendil-works/pi-coding-agent": "0.80.6",
    "@earendil-works/pi-tui": "0.80.6",
    "typescript": "5.8.3"
  },
  "pi": {
    "extensions": [
      "./src/index.ts",
      "./src/context.ts"
    ],
    "skills": [
      "./skills"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KorenKrita/pi-context.git"
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "0.80.6",
    "@earendil-works/pi-ai": "0.80.6",
    "@earendil-works/pi-coding-agent": "0.80.6",
    "@earendil-works/pi-tui": "0.80.6"
  }
}
